
import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (

    <div class="flex items-center justify-center mt-[30px] py-[30px] mb-10">  
    {/* CertifCard; */}
        <img  src='/images/logos_confiance.png' alt="" />
    </div>

    );
});